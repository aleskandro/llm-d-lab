apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: benchmarks
    namespace: experiment-01
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 16Gi
  storageClassName: nfs
  volumeMode: Filesystem