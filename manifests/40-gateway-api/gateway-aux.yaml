apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: infra-gateway
  namespace: openshift-ingress 
spec:
  gatewayClassName: infra-gateway 
  listeners:
  - name: http 
    # TODO: How to handle hostname and certificate?
    hostname: "REPLACE_ME"
    port: 80
    protocol: HTTP
    #tls:
      #mode: Terminate
      #certificateRefs:
      #- name: REPLACE_ME
    allowedRoutes:
      namespaces:
        from: All